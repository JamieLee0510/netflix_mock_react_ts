import React from "react";
import { render, screen } from "@testing-library/react";
import Header from "../../src/components/basic/Header";

test("render the video header", () => {
  render(<Header />);
  // const element = screen.getByD("header")
});
