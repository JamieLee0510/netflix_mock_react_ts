import React from "react";
import { BrowserRouter, Route } from "react-router-dom";

const AppRouter = () => (
  <BrowserRouter>{/* TODO: Nav, Route, Footer */}</BrowserRouter>
);

export default AppRouter;
